---
description: Monorepo 架構與跨層責任邊界規則
alwaysApply: true
---

# Monorepo 架構規範

## 1. 核心分層
- 前端 Angular、後端 C#、資料庫 SQL 規則需分層管理，不可混寫。
- UI、Application、Domain、Infrastructure、Database 各層需維持單一責任。
- 技術細節依賴只能由外向內，不得由 Domain 直接依賴 Infrastructure。

## 2. 路徑與責任邊界
- `apps/frontend/`：主要承載 Angular v20 / Vite 現代化前端應用程式。
- `apps/backend/`：主要承載後端服務容器與 BFF (Backend for Frontend) 進入點。
- `libs/`：共享生態庫，嚴格細分為 `libs/frontend/` (UI 與資料存取)、`libs/backend/` (驗證與資料庫實作) 及 `libs/shared/` (共用型別與公用程式)。
- `services/`、`src/`、`api/`：主要承載 C# / .NET 微服務核心領域與 API 專案。
- `database/`、`migrations/`、`sql/`：主要承載資料表定義、migration 與資料庫腳本。
- CI/CD、容器、部署設定應與業務邏輯分離。

## 3. AI 生成程式碼要求
- 任何新增檔案都必須放在合理目錄，並遵守對應技術規則與 Nx 模組化規範。
- 變更前先閱讀相鄰檔案的命名與實作風格，再產出一致的程式碼。
- 重構時優先維持公開 API、資料契約與 migration 相容性。

## 4. 變更策略
- 小步提交，避免一次跨多層大改。
- API、DB schema、事件格式等公共契約變更時，必須同時提示相容性風險。
- 任何跨服務或跨資料庫的變更，都必須說明影響範圍與回滾策略。