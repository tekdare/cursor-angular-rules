---
description: 專案安全、資料保護、驗證與測試底線
alwaysApply: true
---

# 安全與品質底線

## 1. 安全原則
- 所有輸入都必須驗證與正規化。
- 所有輸出都必須避免洩漏敏感資訊、祕密、連線字串與內部實作細節。
- 所有查詢都必須使用參數化，不得字串拼接 SQL。
- 所有 API 錯誤處理都應回傳安全且一致的錯誤格式。

## 2. 測試與驗證
- 新增或修改功能時，應補齊對應的 Vitest 3 單元測試、整合測試或 migration 驗證。
- 嚴格遵守 ESLint 9 Flat Config (`eslint.config.mjs`) 規範，不可忽略 lint、build、test、type check 或資料庫 migration 驗證。
- 若無法完成測試，必須明確標示未驗證風險與原因。

## 3. 可維護性
- 優先產出可讀、可測、可維護的正式程式碼。
- 禁止大段重複邏輯，應抽出共享元件、服務、helper 或 library。
- 文件、註解與命名應以降低維運成本為目標。

## 4. 合規與稽核
- 不得硬編碼帳號、密碼、token、金鑰、憑證與環境祕密。
- 涉及個資、交易、權限與審計軌跡的修改，必須優先保守處理。
- 任何刪除資料、結構變更、批次更新操作，都必須先提示風險與回復方式。