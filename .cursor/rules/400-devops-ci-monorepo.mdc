---
description: Nx Monorepo 的 DevOps、CI/CD、快取建置、容器與發佈規範
globs:
  - "**/Dockerfile"
  - "**/*.yml"
  - "**/*.yaml"
  - ".github/**/*.yml"
  - ".github/**/*.yaml"
  - "azure-pipelines*.yml"
  - "docker-compose*.yml"
alwaysApply: false
---

# DevOps 與 CI/CD 規範

## 1. Pipeline 原則
- Pipeline 必須可重現、可觀測、可回滾。
- Build、lint、test、security scan、artifact publish 應明確分階段。
- 不可跳過關鍵品質關卡直接部署正式環境。

## 2. Nx Monorepo 策略
- **善用 Nx 快取與增量建置**: 優先使用 `nx affected` 進行受影響範圍分析與快取 (`nx cache`) 機制，避免全倉庫無謂重建。
- 前端 (Vite/Vitest)、後端 (C# / Nest)、資料庫 migration 的驗證流程需獨立且可靈活組合。
- 變更應盡量只影響必要模組，保持跨層解耦。

## 3. 容器與環境
- Dockerfile 需採最小權限與精簡基底映像。
- 環境變數需有命名一致性與敏感資訊隔離。
- 不得把祕密資訊寫入版本庫或映像檔。

## 4. 發佈與稽核
- 發佈前需驗證版本、schema、相容性與回滾方案。
- Migration 與應用部署順序需可控。
- 所有正式變更都應保留追蹤資訊與審計線索。