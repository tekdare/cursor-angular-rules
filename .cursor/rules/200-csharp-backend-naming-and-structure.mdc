---
description: C# 微服務命名、專案結構與現代化 C# 12/13 程式碼規範
globs:
  - "services/**/*.cs"
  - "src/**/*.cs"
  - "api/**/*.cs"
alwaysApply: false
---

# C# 命名與結構規範

## 1. 命名與現代語法
- 類別、record、enum 使用 `PascalCase`。
- 私有欄位使用 `_camelCase`。
- 介面使用 `I` 前綴，例如 `IUserRepository`。
- DTO、Request、Response、Command、Query、Handler 使用明確後綴。
- **擁抱現代 C# 12/13 語法**: 優先採用 File-scoped namespaces、Primary constructors 與 Record structs。

## 2. 結構與 Nx 協作邊界
- 依責任分 Application、Domain、Infrastructure、Api。
- Controller、Endpoint、Handler、Service、Repository 不可混用職責。
- Domain 不直接依賴外部框架與資料庫實作。
- 與 Nx 根目錄下的 `apps/backend` 或 `libs/backend` 進行協作時，需維持清晰的 API 契約與服務界線。

## 3. 程式碼品質
- 開啟 Nullable reference types (`<Nullable>enable</Nullable>`)。
- 禁止 `dynamic` 與過度使用 object。
- 方法需短小、明確，避免超長 God class 或肥大 service。
- 優先用不可變資料結構 (Immutable data structures) 與清楚的 DTO 邊界。

## 4. 依賴注入
- 服務需透過 DI 註冊與注入。
- Service lifetime 需符合使用情境，不可任意全部註冊為 singleton。
- 不可在業務邏輯中直接 new 出外部依賴而繞過 DI。