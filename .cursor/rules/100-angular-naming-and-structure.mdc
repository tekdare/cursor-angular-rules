---
description: Angular 命名、檔案結構與 Angular 現代實作基準
globs:
  - "apps/**/*.ts"
  - "libs/**/*.ts"
alwaysApply: false
---

# Angular 命名與結構規範 (Artifacts)

## 1. 檔案與物件命名

### 1.1 元件 (Components)
- **檔案命名**: 採用 `kebab-case` 搭配 `.component.ts` 後綴（例：`user-profile.component.ts`）。
- **類別命名**: 採用 `PascalCase` 搭配 `Component` 結尾（例：`export class UserProfileComponent {}`）。
- **選擇器 (Selector)**: 必須使用 `kebab-case` 且帶有前綴 `app-`（例：`app-user-profile`）。
- **模板檔**: 同名 `.html`（例：`user-profile.html`）。
- **樣式檔**: 同名 `.css`（例：`user-profile.css`）。

### 1.2 服務 (Services)
- **檔案命名**: `kebab-case` 搭配 `.service.ts`（例：`auth.service.ts`）。
- **類別命名**: `PascalCase` 搭配 `Service` 結尾（例：`export class AuthService {}`）。
- **依賴注入**: 預設採用 `providedIn: 'root'`，僅在特定的 Lazy feature boundary 時例外。

### 1.3 指令 (Directives)
- **檔案命名**: `kebab-case` 搭配 `.directive.ts`（例：`auto-focus.directive.ts`）。
- **類別命名**: `PascalCase` 搭配 `Directive` 結尾。
- **選擇器**: 屬性選擇器使用 `lowerCamelCase`（例：`[appAutoFocus]`）；元素選擇器使用 `app-` 前綴。

### 1.4 管道 (Pipes)
- **檔案命名**: `kebab-case` 搭配 `.pipe.ts`（例：`format-number.pipe.ts`）。
- **類別命名**: `PascalCase` 搭配 `Pipe` 結尾。
- **name 屬性**: `camelCase`（例：`name: 'formatNumber'`）。

---

## 2. Angular v20 現代實作基準

### 2.1 現代化 API 優先
- **全面預設使用 Standalone Components**，摒棄傳統 NgModule 組織模式。
- **禁止使用建構子注入**: 一律改用 `inject()` 函式進行依賴注入（例：`private readonly authService = inject(AuthService)`）。
- **擁抱 Signals 生態**: 優先採用 `input()`、`output()`、`model()`、`signal()`、`computed()` 等最新 Angular v20 API，取代 `@Input` 與 `@Output`。

### 2.2 跨層與後端例外說明
- `apps/backend` 與 `libs/backend` 中的 TypeScript 檔案屬後端微服務或 NestJS 容器範疇，遵循後端對應的架構與依賴注入慣例，不強制套用 Angular 前端專屬元件與模組規範。

---

## 3. 檔案組織與共置
- Component、Template、Style 與 Test 檔案應於同一目錄緊密共置。
- 一個檔案僅包含一個主要類別，維持單一職責。
- 邏輯過於複雜的元件或服務，應重構拆分為更小的獨立元件或共享函式庫 (`libs/frontend`)。