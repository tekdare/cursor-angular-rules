---
description: Monorepo 專案總覽與 AI 協作總則，適用於整個倉庫
alwaysApply: true
---

# 專案總覽

## 1. 專案定位與技術堆疊
- 本倉庫為 AI Rules-Driven 的企業級 Nx Monorepo (v21)。
- 前端採用 Angular v20 搭配 Vite 6 與 AnalogJS；後端採用 C# / .NET 微服務與 Node/Nest 容器架構。
- 測試與品管涵蓋 Vitest 3 測試框架與 ESLint 9 Flat Config 規範。
- 資料層涵蓋 MS-SQL、Oracle、PostgreSQL、MySQL、SQLite。
- 所有 AI 生成內容都必須優先遵守專案既有架構、命名、分層與安全規範。

## 2. AI 協作原則
- 回覆、註解與文件預設使用繁體中文。
- 先給結論，再給步驟、理由與風險。
- 不可臆測不存在的檔案、API、資料表、套件或環境變數。
- 需求不明時，先列出假設與限制，再提出最小可行且安全的修改方案。
- 若全域偏好與專案規則衝突，以專案規則為優先。

## 3. 分層規則載入
- `.cursor/*.mdc` 為專案層級總則。
- `.cursor/rules/*.mdc` 為技術與情境規則。
- 只在符合 `globs` 的檔案或路徑中套用對應規則，避免無關規則污染上下文。

## 4. 禁止事項
- 不得為了快速完成而繞過驗證、型別、安全與測試規範。
- 不得以 placeholder、mock、假資料或 TODO 取代正式實作，除非需求明確指定。
- 不得把某一技術棧的慣例誤套用到其他技術棧，例如把 PostgreSQL 語法當成 MSSQL 通用語法。